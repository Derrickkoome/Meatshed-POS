rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Example: public reads, writes require auth
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}